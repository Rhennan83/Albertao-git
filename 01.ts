//Declaração de valores
const total: number = 7;
const divisor: number = 2;

//Calculo do resto
const resto: number = total % divisor;

//Verificação booleana
const ehpar: boolean = resto === 0;

//Exibição organizada
console.log(`Divisão ${total} / ${divisor}`);
console.log(`Resto da divisão: ${resto}`);
console.log(`É par: ${ehpar}`);



